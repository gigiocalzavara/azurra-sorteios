import {notFound} from "next/navigation";import type {CSSProperties} from "react";import {createClient} from "@/lib/supabase/server";import {PurchaseForm} from "./purchase-form";import styles from "./public-promotion.module.css";export const dynamic="force-dynamic";type Props={params:Promise<{organizationSlug:string;promotionSlug:string}>};type Product={id:string;name:string;description:string;image_url:string|null;price:number};type Promotion={name:string;description:string;special_condition:string|null;campaign_image_url:string|null;quota_quantity:number;quota_price:number;minimum_per_order:number;maximum_per_order:number|null;maximum_per_participant:number|null;reservation_minutes:number;status:string;products:Product[];organization:{name:string;logo_url:string|null;primary_color:string;secondary_color:string};quotas:{available:number;reserved:number;paid:number}};export default async function Page({params}:Props){const {organizationSlug,promotionSlug}=await params,supabase=await createClient(),{data,error}=await supabase.rpc("get_public_promotion",{organization_slug:organizationSlug,promotion_slug:promotionSlug});if(error||!data)notFound();const p=data as Promotion,progress=Math.min(100,Math.round(((p.quotas.paid+p.quotas.reserved)/p.quota_quantity)*100)),brand={"--brand":p.organization.primary_color||"#6900ff","--accent":p.organization.secondary_color||"#00f0ff"} as CSSProperties,max=Math.min(p.maximum_per_order??p.maximum_per_participant??p.quotas.available,p.maximum_per_participant??p.quotas.available,p.quotas.available);return <main className={styles.page} style={brand}><div className={styles.shell}><header className={styles.header}>{p.organization.logo_url?<img src={p.organization.logo_url} alt={p.organization.name}/>:null}<span>{p.organization.name}</span></header><section className={styles.grid}><div className={styles.product}>{p.campaign_image_url?<img src={p.campaign_image_url} alt={p.name}/>:<div>Imagem da campanha</div>}</div><div className={styles.content}><span className={styles.eyebrow}>Promoção ativa</span><h1>{p.name}</h1><p className={styles.description}>{p.description}</p>{p.products?.length?<div className={styles.condition}><strong>Produtos disponíveis ao vencedor</strong>{p.products.map((x,i)=><div key={x.id}>{i+1}. {x.name}</div>)}</div>:null}{p.maximum_per_participant?<div className={styles.condition}>Limite: {p.maximum_per_participant} cotas por participante.</div>:null}<div className={styles.progressMeta}><span>{progress}% reservado</span><span>{p.quotas.available} disponíveis</span></div><div className={styles.progress}><i style={{width:`${progress}%`}}/></div><PurchaseForm organizationSlug={organizationSlug} promotionSlug={promotionSlug} quotaPrice={Number(p.quota_price)} minimum={p.minimum_per_order} maximum={max} reservationMinutes={p.reservation_minutes} disabled={p.status!=="published"||p.quotas.available<p.minimum_per_order}/></div></section><footer className={styles.footer}>Pagamento por PIX • Cotas atribuídas aleatoriamente</footer></div></main>}
