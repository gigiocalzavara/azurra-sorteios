import Link from "next/link";
import {createPromotion} from "./actions";
import {createClient} from "@/lib/supabase/server";
import styles from "../../dashboard.module.css";
type Props={searchParams:Promise<{error?:string}>};
export default async function Page({searchParams}:Props){const {error}=await searchParams,supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();const {data:membership}=user?await supabase.from("organization_members").select("organization_id").eq("user_id",user.id).limit(1).maybeSingle():{data:null};const {data:products}=membership?await supabase.from("products").select("id,name,price").eq("organization_id",membership.organization_id).eq("active",true).order("name"):{data:[]};return <main className={styles.main}><div className={styles.heading}><div><h1>Nova promoção</h1><p>Monte a campanha, escolha os produtos e configure as cotas.</p></div></div>{error?<div className={styles.alertError}>{error}</div>:null}<form className={styles.form} action={createPromotion}><section className={styles.section}><h2>Campanha</h2><div className={styles.fields}><label className={`${styles.field} ${styles.full}`}>Nome da campanha<input name="name" required placeholder="Ex.: Sorteio de perfumes de setembro"/></label><label className={`${styles.field} ${styles.full}`}>Texto da campanha<textarea name="description" required placeholder="Apresente a promoção e explique como participar"/></label><label className={`${styles.field} ${styles.full}`}>Foto da campanha<input type="file" name="campaignImage" accept="image/png,image/jpeg,image/webp" required/><small>JPG, PNG ou WEBP, com até 5 MB.</small></label></div></section><section className={styles.section}><h2>Produtos</h2><p>Selecione um ou mais produtos que o vencedor poderá escolher.</p><div className={styles.fields}>{(products||[]).length?(products||[]).map((p:any)=><label className={styles.field} key={p.id} style={{display:"flex",flexDirection:"row",alignItems:"center",gap:10}}><input type="checkbox" name="productIds" value={p.id}/><span>{p.name} — {Number(p.price).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</span></label>):<div className={styles.alertError}>Cadastre pelo menos um produto antes de criar a promoção.</div>}</div></section><section className={styles.section}><h2>Cotas e prêmio alternativo</h2><div className={styles.fields}><label className={styles.field}>Quantidade de cotas<input type="number" name="quotaQuantity" min="1" max="100000" required/></label><label className={styles.field}>Valor por cota<input type="number" name="quotaPrice" min="0.01" step="0.01" required/></label><label className={styles.field}>Limite de cotas por participante <small>Opcional</small><input type="number" name="maximumPerParticipant" min="1" placeholder="Sem limite"/></label><label className={styles.field}>PIX para pós-sorteio<input type="number" name="postDrawPixAmount" min="0" step="0.01" required placeholder="Valor oferecido ao vencedor"/></label><label className={styles.field}>Mínimo por compra<input type="number" name="minimumPerOrder" min="1" defaultValue="1" required/></label><label className={styles.field}>Reserva em minutos<input type="number" name="reservationMinutes" min="5" max="1440" defaultValue="30" required/></label></div></section><div className={styles.actions}><Link href="/dashboard/promocoes">Cancelar</Link><button className={styles.primary} type="submit" disabled={!(products||[]).length}>Salvar promoção</button></div></form></main>}
