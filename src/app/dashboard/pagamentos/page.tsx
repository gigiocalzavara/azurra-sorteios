import {createClient} from "@/lib/supabase/server";
import styles from "../dashboard.module.css";
import {confirmPayment} from "./actions";
const labels:Record<string,string>={pending:"Aguardando PIX",payment_reported:"Pagamento informado",paid:"Confirmado",expired:"Expirado",cancelled:"Cancelado",payment_conflict:"Conflito"};
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const supabase=await createClient();const {data:orders}=await supabase.from("orders").select("id,status,quota_count,total_amount,created_at,payment_reported_at,participants(name,phone_e164),promotions(name)").order("created_at",{ascending:false});return <main className={styles.main}><div className={styles.heading}><div><h1>Pagamentos</h1><p>Confira os avisos de PIX e confirme manualmente o recebimento.</p></div></div>{error?<div className={styles.alertError}>{error}</div>:null}<section className={styles.panel}>{!orders?.length?<div className={styles.empty}>Nenhum pedido recebido.</div>:<div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Participante</th><th>Promoção</th><th>Cotas</th><th>Valor</th><th>Status</th><th></th></tr></thead><tbody>{orders.map(o=>{const participant=Array.isArray(o.participants)?o.participants[0]:o.participants,promotion=Array.isArray(o.promotions)?o.promotions[0]:o.promotions;return <tr key={o.id}><td><strong>{participant?.name}</strong><br/><small>{participant?.phone_e164}</small></td><td>{promotion?.name}</td><td>{o.quota_count}</td><td>{Number(o.total_amount).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</td><td><span className={styles.status}>{labels[o.status]||o.status}</span></td><td>{["pending","payment_reported"].includes(o.status)?<form action={confirmPayment}><input type="hidden" name="id" value={o.id}/><button className={styles.smallButton}>Confirmar PIX</button></form>:null}</td></tr>})}</tbody></table></div>}</section></main>}
