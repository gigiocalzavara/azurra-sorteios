import {createClient} from "@/lib/supabase/server";
import {createProduct} from "./actions";
import styles from "../dashboard.module.css";
type Props={searchParams:Promise<{error?:string;created?:string}>};
export default async function ProductsPage({searchParams}:Props){const params=await searchParams,supabase=await createClient(),{data:{user}}=await supabase.auth.getUser();const {data:membership}=user?await supabase.from("organization_members").select("organization_id").eq("user_id",user.id).limit(1).maybeSingle():{data:null};const {data:products}=membership?await supabase.from("products").select("id,name,description,image_url,price,active").eq("organization_id",membership.organization_id).order("created_at",{ascending:false}):{data:[]};return <main className={styles.main}><div className={styles.heading}><div><h1>Produtos</h1><p>Cadastre os produtos que poderão ser oferecidos nas promoções.</p></div></div>{params.error?<div className={styles.alertError}>{params.error}</div>:null}{params.created?<div className={styles.alertSuccess}>Produto cadastrado.</div>:null}<form className={styles.form} action={createProduct}><section className={styles.section}><h2>Novo produto</h2><div className={styles.fields}><label className={styles.field}>Nome<input name="name" required placeholder="Perfume X"/></label><label className={styles.field}>Preço<input name="price" type="number" min="0" step="0.01" required placeholder="299,90"/></label><label className={`${styles.field} ${styles.full}`}>Descrição<textarea name="description" required placeholder="Descrição do produto"/></label><label className={`${styles.field} ${styles.full}`}>Foto<input name="image" type="file" accept="image/png,image/jpeg,image/webp" required/><small>JPG, PNG ou WEBP, até 5 MB.</small></label></div><div className={styles.actions}><button className={styles.primary}>Cadastrar produto</button></div></section></form><section className={styles.section}><h2>Produtos cadastrados</h2><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Produto</th><th>Descrição</th><th>Preço</th><th>Status</th></tr></thead><tbody>{(products||[]).map((p:any)=><tr key={p.id}><td>{p.name}</td><td>{p.description}</td><td>{Number(p.price).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</td><td>{p.active?"Ativo":"Inativo"}</td></tr>)}</tbody></table></div></section></main>}
