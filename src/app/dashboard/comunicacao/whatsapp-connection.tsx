"use client";
import {useEffect,useState} from "react";
import local from "./communication.module.css";
type Group={id:string;subject:string;participants:number};
export default function WhatsAppConnection({organizationId,promotionId,currentGroup}:{organizationId:string;promotionId?:string;currentGroup?:string|null}){const [status,setStatus]=useState("disconnected"),[qr,setQr]=useState<string|null>(null),[phone,setPhone]=useState<string|null>(null),[groups,setGroups]=useState<Group[]>([]),[selected,setSelected]=useState(currentGroup||""),[message,setMessage]=useState("");const call=async(url:string,options?:RequestInit)=>{const response=await fetch(url,options),data=await response.json();if(!response.ok)throw new Error(data.error||"Erro na conexão");return data};const refresh=async()=>{try{const data=await call(`/api/whatsapp?org=${organizationId}&action=status`);setStatus(data.status);setQr(data.qr);setPhone(data.phone);if(data.status==="connected"){const list=await call(`/api/whatsapp?org=${organizationId}&action=groups`);setGroups(list)}}catch(e){setMessage(e instanceof Error?e.message:"Falha na conexão")}};useEffect(()=>{refresh();const timer=setInterval(refresh,4000);return()=>clearInterval(timer)},[organizationId]);const connect=async()=>{setMessage("Gerando QR Code...");try{const data=await call("/api/whatsapp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({org:organizationId,action:"connect"})});setStatus(data.status);setQr(data.qr);setMessage("")}catch(e){setMessage(e instanceof Error?e.message:"Falha")}};const saveGroup=async()=>{const group=groups.find(g=>g.id===selected);if(!group||!promotionId)return;try{await call("/api/whatsapp",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({org:organizationId,action:"select_group",promotionId,groupId:group.id,groupName:group.subject})});setMessage(`Grupo ${group.subject} vinculado à promoção.`)}catch(e){setMessage(e instanceof Error?e.message:"Falha")}};return <section className={local.connection}><div className={local.connectionHead}><div><strong>WhatsApp do operador</strong><p>{status==="connected"?`Conectado${phone?` • ${phone}`:""}`:status==="qr"?"Aguardando leitura do QR Code":"Desconectado"}</p></div><span className={status==="connected"?local.online:local.offline}>{status==="connected"?"Online":"Offline"}</span></div>{status!=="connected"?<button onClick={connect}>Conectar WhatsApp</button>:null}{qr&&status!=="connected"?<div className={local.qr}><img src={qr} alt="QR Code do WhatsApp"/><p>No celular do operador, abra WhatsApp → Aparelhos conectados → Conectar aparelho.</p></div>:null}{status==="connected"&&promotionId?<div className={local.groupPicker}><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">Selecione o grupo da promoção</option>{groups.map(g=><option value={g.id} key={g.id}>{g.subject} ({g.participants})</option>)}</select><button onClick={saveGroup} disabled={!selected}>Vincular grupo</button></div>:null}{message?<small>{message}</small>:null}</section>}
