import {createClient} from "@/lib/supabase/server";
import styles from "../dashboard.module.css";
export default async function Page(){const {data:participants}=await (await createClient()).from("participants").select("id,name,phone_e164,consent_at,created_at").order("created_at",{ascending:false});return <main className={styles.main}><div className={styles.heading}><div><h1>Participantes</h1><p>Pessoas que reservaram cotas nas suas promoções.</p></div></div><section className={styles.panel}>{!participants?.length?<div className={styles.empty}>Nenhum participante cadastrado.</div>:<div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Nome</th><th>WhatsApp</th><th>Cadastro</th></tr></thead><tbody>{participants.map(p=><tr key={p.id}><td><strong>{p.name}</strong></td><td>{p.phone_e164}</td><td>{new Date(p.created_at).toLocaleString("pt-BR")}</td></tr>)}</tbody></table></div>}</section></main>}
